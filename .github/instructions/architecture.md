feature based architecture

src/components -> shared components that can be used across the app (buttons, inputs, etc)
src/features -> features of the app, each feature has its own folder with its components, hooks, etc (tasks, users, etc)
src/routes -> routes of the app, each route has its own folder with its components, hooks, etc (home, about, etc)